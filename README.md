# Nek.12's Homebrew tap

Personal Homebrew packages with pinned checksums and upstream release artifacts.

```sh
brew tap Nek-12/tap
brew trust --cask Nek-12/tap/qbittorrent
brew install --cask Nek-12/tap/qbittorrent
```
